# Groundwork

A single-file, self-contained study app. No build step, no dependencies —
everything lives in `index.html`.

## Updating

Replace `index.html` with the new version, then run:

```bash
git add -A && git commit -m "update" && git push
```

GitHub Pages picks up the change automatically within a minute or two.
